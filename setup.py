#!/usr/bin/env python

from distutils.core import setup

setup(name='django_resume',
      version='0.1',
      description='Django Resume is a Django app built to generate a resume on a user\'s site.',
      author='Chris Kelly, Rob Beagrie',
      author_email='rob@beagrie.com',
      url='https://github.com/rbeagrie/django-resume',
      packages=['django_resume'],
     )
